# 📖 دليل الاستخدام المفصل - عداد الأذكار والتسبيح

## 🎯 نظرة عامة

تطبيق عداد الأذكار والتسبيح هو تطبيق إسلامي مصمم لمساعدة المسلمين على تتبع أذكارهم اليومية بطريقة سهلة وبسيطة. التطبيق يعمل بالكامل بدون إنترنت ويحفظ جميع بياناتك تلقائياً.

---

## 🚀 البدء السريع

### خطوات التشغيل الأولى

#### 1. تثبيت البرامج المطلوبة

**أ) تثبيت Node.js:**
1. اذهب إلى: https://nodejs.org/
2. حمل النسخة LTS (الموصى بها)
3. قم بتثبيتها باتباع التعليمات
4. للتأكد من التثبيت، افتح Terminal واكتب:
   ```bash
   node --version
   npm --version
   ```

**ب) تثبيت Expo CLI:**
```bash
npm install -g expo-cli
```

#### 2. تجهيز المشروع

```bash
# انتقل لمجلد المشروع
cd dhikr-app

# ثبت المكتبات
npm install
```

#### 3. تشغيل التطبيق

**للتجربة السريعة على المتصفح:**
```bash
npm run web
```

---

## 📱 تشغيل التطبيق على الهاتف

### طريقة 1: باستخدام Expo Go (الأسهل - بدون تثبيت)

#### للأندرويد:
1. حمل تطبيق **Expo Go** من Google Play Store
2. شغل التطبيق من Terminal:
   ```bash
   npm start
   ```
3. افتح Expo Go على هاتفك
4. امسح رمز QR الظاهر على الشاشة
5. التطبيق سيفتح مباشرة!

#### للآيفون:
1. حمل تطبيق **Expo Go** من App Store
2. شغل التطبيق من Terminal:
   ```bash
   npm start
   ```
3. افتح تطبيق الكاميرا على الآيفون
4. وجه الكاميرا على رمز QR
5. اضغط على الإشعار للفتح في Expo Go

### طريقة 2: بناء ملف APK (أندرويد)

**لتثبيت التطبيق بشكل دائم على الهاتف:**

```bash
# سجل دخول (إذا لم تكن مسجل)
expo login

# ابني ملف APK
expo build:android -t apk
```

- ستُسأل عدة أسئلة، اضغط Enter للإجابات الافتراضية
- البناء سيأخذ 10-15 دقيقة
- ستحصل على رابط لتحميل ملف APK
- حمل الملف على هاتفك وثبته

---

## 🎮 كيفية استخدام التطبيق

### الشاشة الرئيسية

#### 1. اختيار الذكر
في أعلى الشاشة، ستجد قائمة منسدلة بها:
- سبحان الله
- الحمد لله  
- الله أكبر
- لا إله إلا الله
- أستغفر الله
- لا حول ولا قوة إلا بالله

**كيف تختار:**
1. اضغط على القائمة المنسدلة
2. اختر الذكر المراد
3. سيظهر الذكر المختار بخط كبير أخضر

#### 2. العد

**الزر الدائري الأخضر الكبير (+):**
- اضغطه لزيادة العدد
- كل ضغطة = +1
- العداد يظهر فوق الزر بخط كبير

**ملاحظات مهمة:**
- ✅ الحفظ تلقائي - لا تقلق
- ✅ يمكنك إغلاق التطبيق والعودة لاحقاً
- ✅ البيانات محفوظة محلياً

#### 3. إعادة التعيين

**زر "إعادة تعيين العداد" (برتقالي):**
- يصفر العداد الحالي فقط
- لا يؤثر على الإحصائيات المحفوظة
- سيطلب تأكيد قبل التنفيذ

**مثال للاستخدام:**
- عدت "سبحان الله" 33 مرة
- اضغط إعادة تعيين
- العداد يرجع لـ 0
- لكن الـ 33 محفوظة في الإحصائيات

#### 4. مسح البيانات

**زر "مسح جميع البيانات" (أحمر):**
- يحذف كل شيء
- يصفر جميع الإحصائيات
- يعيد التطبيق للبداية
- ⚠️ **تحذير:** لا يمكن التراجع!

#### 5. الإحصائيات

**في أسفل الشاشة:**
- جدول يعرض إجمالي كل ذكر
- "الإجمالي الكلي" لجميع الأذكار
- الأرقام محدثة فورياً

---

## 💡 نصائح الاستخدام

### للاستخدام اليومي:

**1. أذكار الصباح والمساء:**
```
- اختر "سبحان الله"
- اعدها 33 مرة
- اضغط إعادة تعيين
- اختر "الحمد لله"
- اعدها 33 مرة
- وهكذا...
```

**2. التسبيح بعد الصلاة:**
```
- لكل ذكر 33 مرة
- استخدم إعادة التعيين بين كل ذكر
- الإحصائيات تحفظ المجموع
```

**3. الاستغفار:**
```
- اختر "أستغفر الله"
- اعده 100 مرة يومياً
- التطبيق يحفظ إجمالي الشهر
```

### ميزات إضافية:

**حفظ تلقائي:**
- لا حاجة لزر "حفظ"
- كل ضغطة محفوظة فوراً
- يمكنك إغلاق التطبيق أي وقت

**عمل بدون إنترنت:**
- لا يحتاج إنترنت أبداً
- البيانات على جهازك فقط
- خصوصيتك محمية 100%

---

## 🎨 شرح الواجهة

### الألوان ومعانيها:

- 🟢 **الأخضر**: الزر الرئيسي والأرقام الإيجابية
- 🟠 **البرتقالي**: إعادة التعيين (تحذير خفيف)
- 🔴 **الأحمر**: مسح البيانات (تحذير قوي)
- ⚪ **الأبيض/الرمادي**: النصوص العامة
- 🔵 **الأزرق الداكن**: الخلفيات (راحة للعين)

### حجم الخطوط:

- **72px**: العداد الرئيسي (كبير جداً)
- **32px**: الذكر المختار
- **18-22px**: النصوص والإحصائيات
- **14px**: النصوص الثانوية

جميع الأحجام مختارة لسهولة القراءة في أي إضاءة.

---

## 🔧 استكشاف الأخطاء الشائعة

### مشكلة 1: التطبيق لا يفتح

**الأعراض:**
- خطأ عند تشغيل `npm start`
- شاشة بيضاء

**الحلول:**
```bash
# 1. احذف المجلدات وأعد التثبيت
rm -rf node_modules
npm install

# 2. امسح الكاش
expo start -c

# 3. حدث Expo
npm install -g expo-cli@latest
```

### مشكلة 2: القائمة المنسدلة لا تعمل

**الأعراض:**
- لا يمكن تغيير الذكر
- القائمة لا تفتح

**الحلول:**
```bash
# أعد تثبيت المكتبة
npm uninstall react-native-picker-select
npm install react-native-picker-select

# أعد تشغيل التطبيق
npm start
```

### مشكلة 3: البيانات لا تحفظ

**الأعراض:**
- العداد يرجع لـ 0 بعد إغلاق التطبيق

**الحلول:**
```bash
# تأكد من تثبيت AsyncStorage
npm install @react-native-async-storage/async-storage

# على Android: تأكد من الصلاحيات
# اذهب لإعدادات التطبيق > الصلاحيات > التخزين
```

### مشكلة 4: رمز QR لا يعمل

**الأعراض:**
- Expo Go لا يفتح التطبيق

**الحلول:**
1. تأكد أن الهاتف والكمبيوتر على نفس الشبكة
2. جرب وضع Tunnel:
   ```bash
   expo start --tunnel
   ```
3. حدث Expo Go على الهاتف

### مشكلة 5: خطأ في البناء (APK)

**الأعراض:**
- فشل `expo build:android`

**الحلول:**
1. تأكد من تسجيل الدخول:
   ```bash
   expo login
   ```
2. جرب بناء AAB بدلاً من APK:
   ```bash
   expo build:android -t app-bundle
   ```

---

## 🌟 أسئلة شائعة (FAQ)

### س: هل يحتاج التطبيق إنترنت؟
**ج:** لا أبداً. التطبيق يعمل بالكامل بدون إنترنت.

### س: أين تحفظ البيانات؟
**ج:** محلياً على جهازك فقط باستخدام AsyncStorage.

### س: هل بياناتي آمنة؟
**ج:** نعم 100%. لا يتم إرسال أي بيانات لأي خادم.

### س: هل يمكن تغيير الألوان؟
**ج:** نعم، يمكنك تعديل ملف `App.js` في قسم `styles`.

### س: كم يستهلك من البطارية؟
**ج:** قليل جداً، التطبيق بسيط ولا يعمل في الخلفية.

### س: هل يمكن تصدير البيانات؟
**ج:** حالياً لا، لكن يمكن إضافة هذه الميزة لاحقاً.

### س: كيف أحذف التطبيق؟
**ج:** مثل أي تطبيق آخر، احذفه من الجهاز.

### س: الخط صغير/كبير جداً؟
**ج:** يمكن تعديل أحجام الخطوط في ملف `App.js`.

---

## 📊 سيناريوهات الاستخدام

### سيناريو 1: المسلم المبتدئ

**الهدف:** البدء بالأذكار اليومية

```
1. افتح التطبيق
2. اختر "سبحان الله"
3. اضغط الزر 10 مرات
4. لاحظ الرقم يزيد
5. غداً كرر مع زيادة العدد
```

### سيناريو 2: حافظ الأوراد

**الهدف:** ورد يومي ثابت (100 من كل ذكر)

```
1. سبحان الله × 100
   - اضغط الزر 100 مرة
   - اضغط إعادة تعيين
   
2. الحمد لله × 100
   - اضغط الزر 100 مرة
   - اضغط إعادة تعيين
   
3. كرر لبقية الأذكار
4. راجع الإحصائيات في النهاية
```

### سيناريو 3: تحدي رمضان

**الهدف:** 10,000 تسبيحة في رمضان

```
1. افتح الإحصائيات
2. راقب "الإجمالي الكلي"
3. كل يوم اعد ~333 مرة
4. في نهاية الشهر ستصل 10,000
```

---

## 🔐 الخصوصية والأمان

### ما نجمعه:
- ❌ لا شيء! صفر بيانات

### ما نحفظه:
- ✅ فقط عدد الأذكار على جهازك

### هل نتتبع الموقع؟
- ❌ لا أبداً

### هل نطلب صلاحيات؟
- ✅ فقط التخزين المحلي

### هل يمكن استرجاع البيانات؟
- ⚠️ إذا حذفت التطبيق، تذهب البيانات معه

---

## 🛠️ تخصيص التطبيق (للمطورين)

### تغيير الألوان:

افتح `App.js` واذهب لـ `styles`:

```javascript
const styles = StyleSheet.create({
  // غير هذه القيم:
  container: {
    backgroundColor: '#0d1b2a', // لون الخلفية الرئيسي
  },
  incrementButton: {
    backgroundColor: '#4CAF50', // لون الزر الرئيسي
  },
  // ... إلخ
});
```

### إضافة أذكار جديدة:

في `App.js`، عدل مصفوفة `ADHKAR`:

```javascript
const ADHKAR = [
  { label: 'سبحان الله', value: 'subhanallah' },
  { label: 'الحمد لله', value: 'alhamdulillah' },
  // أضف هنا:
  { label: 'ذكر جديد', value: 'new_dhikr' },
];
```

لا تنسَ إضافة القيمة في `totals`:

```javascript
const [totals, setTotals] = useState({
  subhanallah: 0,
  alhamdulillah: 0,
  // أضف هنا:
  new_dhikr: 0,
});
```

### تغيير حجم الخط:

```javascript
counterText: {
  fontSize: 72, // غير هذا الرقم
  // ... بقية الأنماط
},
```

---

## 📞 الحصول على المساعدة

### لديك مشكلة؟

1. **راجع قسم استكشاف الأخطاء** أعلاه
2. **تأكد من آخر تحديث:**
   ```bash
   npm install
   ```
3. **امسح الكاش وأعد التشغيل:**
   ```bash
   expo start -c
   ```

### لا يزال لا يعمل؟

- افتح Issue على GitHub
- اشرح المشكلة بالتفصيل
- أرفق لقطة شاشة إن أمكن

---

## 🎓 للمطورين: شرح الكود

### الهيكل العام:

```javascript
// 1. الحالات (States)
const [currentCount, setCurrentCount] = useState(0);
const [selectedDhikr, setSelectedDhikr] = useState('subhanallah');
const [totals, setTotals] = useState({...});

// 2. التأثيرات (Effects)
useEffect(() => loadData(), []); // عند البدء
useEffect(() => saveData(), [...]); // عند التغيير

// 3. الدوال (Functions)
const loadData = async () => {...}; // تحميل من AsyncStorage
const saveData = async () => {...}; // حفظ في AsyncStorage
const incrementCount = () => {...}; // زيادة العداد

// 4. الواجهة (UI)
return <View>...</View>;
```

### حفظ البيانات:

```javascript
// الحفظ
await AsyncStorage.setItem(key, value);

// التحميل
const data = await AsyncStorage.getItem(key);

// المسح
await AsyncStorage.removeItem(key);
```

### RTL Support:

```javascript
I18nManager.allowRTL(true);
I18nManager.forceRTL(true);
```

هذا يجعل الواجهة من اليمين لليسار تلقائياً.

---

## 📚 مصادر إضافية

### تعلم المزيد:

- **React Native Docs:** https://reactnative.dev/
- **Expo Docs:** https://docs.expo.dev/
- **AsyncStorage Guide:** https://react-native-async-storage.github.io/

### أدوات مفيدة:

- **Expo Snack** (تجربة أونلاين): https://snack.expo.dev/
- **React Native Directory** (مكتبات): https://reactnative.directory/

---

## 🤲 ختاماً

نسأل الله أن ينفع بهذا التطبيق، وأن يجعله في ميزان حسنات كل من ساهم فيه.

**اللهم اجعل عملنا خالصاً لوجهك الكريم**

---

*آخر تحديث: نوفمبر 2024*

**بارك الله فيكم** 🌟
